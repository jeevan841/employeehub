export default function Avatar({ firstName='', lastName='', profileImage='', size='medium' }) { const initials = `${firstName?.[0] || ''}${lastName?.[0] || ''}`.toUpperCase() || '?'; return profileImage ? <img className={`avatar avatar-${size}`} src={profileImage} alt={`${firstName} ${lastName}`} /> : <span className={`avatar avatar-${size}`} aria-label={`${firstName} ${lastName}`}>{initials}</span>; }
